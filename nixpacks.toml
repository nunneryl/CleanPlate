# nixpacks.toml

# This section tells Railway's build system (Nixpacks) to add extra system packages.
[phases.setup]
# We are adding "postgresql-client", which includes the 'psql' command.
aptPkgs = ["postgresql-client"]